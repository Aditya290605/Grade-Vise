import 'package:flutter/material.dart';

class UsesList extends StatelessWidget {
  const UsesList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
